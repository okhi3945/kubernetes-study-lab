apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx-deploy
spec:
  type: ClusterIP
  selector:
    app: nginx-deploy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
