apiVersion: v1
kind: Service
metadata:
  name: nginx-svc-bad
spec:
  type: ClusterIP
  selector:
    app: wrong-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
